🧩 FINALIZE DIGITAL CARD LEAD INTELLIGENCE SYSTEM

Stop adding new features and stabilize the existing implementation into a production-ready system.

1. SINGLE SOURCE OF TRUTH

Ensure ALL of the following are unified under one model:

card data
sessions
events
lead scoring

No duplicate counters anywhere in frontend or backend.

Remove any double counting logic (especially view tracking).

2. FIX ANALYTICS CONSISTENCY

Ensure:

/api/track/session is the ONLY place that increments views
/api/track/event must NEVER modify view counts
frontend must NOT increment analytics locally
3. FINAL LEAD MODEL

Each session must produce:

sessionId
cardId
timestamps (firstSeen, lastSeen)
scrollDepthMax
totalTimeOnPage
event breakdown (clicks, copies, etc.)
intentScore (0–100)
leadStatus (cold / warm / hot)
4. LEAD INTENT SCORE MUST BE DETERMINISTIC

Fix scoring so it is predictable and not duplicated:

Example formula:

+40 if time > 20s
+30 if scroll > 75%
+20 if any contact click
+10 if return visit

NO overlapping or double increments anywhere.

5. ANALYTICS DASHBOARD (FINAL FORM)

Must include:

Total Views (unique sessions only)
Unique Visitors
Avg Time on Page
Conversion Events
Lead Table (sorted by intentScore descending)

Lead Table columns:

Visitor ID
Score
Status (HOT / WARM / COLD)
Top Action (email / call / site / scroll)
6. IMAGE UPLOAD SYSTEM (CLEANUP)

Ensure:

profile image upload
cover upload
logo upload
all return secure URL
no duplicate upload handlers exist
all image inputs replaced with upload component
7. PUBLIC CARD PAGE MUST MATCH BUILDER

Ensure:

same layout system
same theme system
same data source
same SEO meta injection
no static fallback values
8. REMOVE REDUNDANT OR EXPERIMENTAL CODE

Clean up:

duplicate tracking functions
unused analytics components
overlapping event listeners
old view counters
FINAL GOAL

This system must behave like:

“HubSpot-style lead tracking inside a digital business card”

Every visit = trackable lead
Every click = intent signal
Every session = scored and stored

⚠️ What I’d watch for next (important)

Once this finishes, your next bottleneck will be:

real-time updates (WebSockets)
scaling analytics queries
lead deduplication (same user multiple sessions)

But don’t touch that yet — get THIS stable first.